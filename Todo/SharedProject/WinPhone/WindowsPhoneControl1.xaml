<!-- <MyReorderListBox x:Class="Todo.WinPhone.MyReorderListBox"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:local="clr-namespace:Todo.WinPhone"
    mc:Ignorable="d"
    FontFamily="{StaticResource PhoneFontFamilyNormal}"
    FontSize="{StaticResource PhoneFontSizeNormal}"
    Foreground="{StaticResource PhoneForegroundBrush}"
    d:DesignHeight="480" d:DesignWidth="480">

    <MyReorderListBox.Resources>
        <local:StatusToImageSourceConverterWP x:Key="VisibilityConverter"/>
    </MyReorderListBox.Resources>


    <DataTemplate>
        <StackPanel Orientation="Horizontal">

            <Setter Property="Background" Value="Black" />
            <Style.Triggers>
                <DataTrigger Binding="{Binding IsSelected}" Value="True">
                    <Setter Property="Background" Value="White"/>
                </DataTrigger>

                <Image Width="52" Height="52" Source="{Binding Path=Status }" />
                    <TextBlock Text="{Binding Path=Name}" Margin="12,4,12,4" FontSize="26" TextTrimming="WordEllipsis"></TextBlock>
        </StackPanel>
    </DataTemplate>

</MyReorderListBox> -->